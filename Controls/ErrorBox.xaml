<UserControl x:Class="Tools.Controls.ErrorBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:Tools.Converters"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <!-- 转换器 -->
        <converters:BooleanToVisibleConverter x:Key="BooleanToVisibleConverter" />
    </UserControl.Resources>
    <Grid Margin="0,100,0,60"
          Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibleConverter}, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <StackPanel HorizontalAlignment="Center">
            <ui:SymbolIcon Symbol="DismissCircle48"
                           Width="48"
                           Height="48"
                           FontSize="48"
                           Foreground="#FF5252"
                           Margin="0,0,0,16"
                           HorizontalAlignment="Center" />

            <!-- 错误信息文本 -->
            <ui:TextBlock Text="{Binding ErrorMessage, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          FontSize="20"
                          FontWeight="Bold"
                          Foreground="White"
                          HorizontalAlignment="Center"
                          TextWrapping="Wrap"
                          TextAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
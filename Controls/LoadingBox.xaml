<UserControl x:Class="Tools.Controls.LoadingBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:Tools.Converters"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <UserControl.Resources>
        <!-- 转换器 -->
        <converters:BooleanToVisibleConverter x:Key="BooleanToVisibleConverter" />
    </UserControl.Resources>
    <Grid HorizontalAlignment="Center"
          Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibleConverter}, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ui:ProgressRing Grid.Row="0"
                         IsIndeterminate="{Binding IsActive, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         Margin="0,100,0,0" />

        <ui:TextBlock Grid.Row="1"
                      Text="{Binding LoadingMessage, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      Margin="0,20,0,60"
                      FontSize="20"
                      FontWeight="Bold"
                      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                      TextWrapping="Wrap"
                      TextAlignment="Center" />
    </Grid>
</UserControl>
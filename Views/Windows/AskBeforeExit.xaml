<ui:FluentWindow x:Class="Tools.Views.Windows.AskBeforeExit"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:viewModel="clr-namespace:Tools.ViewModel.AskBeforeExitDialog"
                 mc:Ignorable="d"
                 Title="AskBeforeExit"
                 MaxHeight="150"
                 MaxWidth="300"
                 MinHeight="150"
                 MinWidth="300"
                 d:DataContext="{d:DesignInstance Type=viewModel:AskBeforeExitViewModel}"
                 d:DesignHeight="150"
                 d:DesignWidth="300"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 WindowStartupLocation="CenterOwner"
                 ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- 标题 -->
        <ui:TextBlock Grid.Row="0" Text="是否直接退出程序" TextAlignment="Center" FontTypography="Subtitle" />

        <!-- 是否不再询问 -->
        <CheckBox Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                  IsChecked="{Binding DoNotAskAgain}" Content="不再询问" />

        <!-- 按钮 -->
        <Grid Grid.Row="2" Width="200" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0" Content="取消" Command="{Binding CancelCommand}" />
            <ui:Button Grid.Column="2" Content="隐藏" Command="{Binding HideCommand}" />
            <ui:Button Grid.Column="4" Content="确定" Command="{Binding ExitCommand}" Appearance="Primary" />
        </Grid>
    </Grid>
</ui:FluentWindow>
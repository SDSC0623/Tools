<ui:FluentWindow x:Class="Tools.Views.Windows.GetTextsDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:viewModel="clr-namespace:Tools.ViewModel.GetTextsDialogViewModel"
                 Title="GetTextsDialog"
                 mc:Ignorable="d"
                 MinHeight="400"
                 MinWidth="500"
                 MaxHeight="400"
                 MaxWidth="500"
                 d:DesignHeight="400"
                 d:DesignWidth="500"
                 d:DataContext="{d:DesignInstance viewModel:GetTextsDialogViewModel}"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 WindowStartupLocation="CenterOwner"
                 ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- 标题 -->
        <ui:TextBlock Grid.Row="0" Text="{Binding Title}"
                      TextAlignment="Center" FontTypography="Title" Margin="0,0,0,20" />

        <!-- 内容 -->
        <ui:TextBlock Grid.Row="1" Text="{Binding Content}"
                      FontTypography="BodyStrong" TextWrapping="Wrap" Margin="0,0,0,10" />

        <ui:DynamicScrollViewer Grid.Row="2">
            <Grid>
                <ItemsControl ItemsSource="{Binding Texts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Border Margin="0,0,0,10" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ui:TextBlock Grid.Column="0" Text="{Binding PlaceHolderAndName}"
                                                  FontTypography="Body" VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Margin="0,0,5,0" />
                                    <ui:TextBox Grid.Column="1" VerticalAlignment="Center"
                                                Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                                                PlaceholderText="{Binding PlaceHolderAndName}" />
                                </Grid>
                            </ui:Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ui:DynamicScrollViewer>

        <!-- 按钮 -->
        <Grid Grid.Row="3" Width="200" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0" Content="取消" Command="{Binding CancelCommand}" />
            <ui:Button Grid.Column="2" Content="确定" Command="{Binding ConfirmCommand}" Foreground="White"
                       Appearance="Primary" />
        </Grid>
    </Grid>
</ui:FluentWindow>